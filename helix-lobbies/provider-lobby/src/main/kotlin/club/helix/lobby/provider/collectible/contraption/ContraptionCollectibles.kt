package club.helix.lobby.provider.collectible.contraption

import club.helix.lobby.provider.collectible.contraption.provider.ExplosiveSheepContraption

enum class ContraptionCollectibles(val collectible: ExplosiveSheepContraption) {
    EXPLOSIVE_SHEEP(ExplosiveSheepContraption())
}